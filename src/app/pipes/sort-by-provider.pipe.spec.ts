import { SortByProviderPipe } from './sort-by-provider.pipe';

describe('SortByProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
