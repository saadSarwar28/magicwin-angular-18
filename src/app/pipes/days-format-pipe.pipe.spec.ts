import { DaysFormatPipePipe } from './days-format-pipe.pipe';

describe('DaysFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
