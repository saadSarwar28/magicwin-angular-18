import { MarkettypePipe } from './markettype.pipe';

describe('MarkettypePipe', () => {
  it('create an instance', () => {
    const pipe = new MarkettypePipe();
    expect(pipe).toBeTruthy();
  });
});
