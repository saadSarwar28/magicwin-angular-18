import { EventtypeidPipe } from './eventtypeid.pipe';

describe('EventtypeidPipe', () => {
  it('create an instance', () => {
    const pipe = new EventtypeidPipe();
    expect(pipe).toBeTruthy();
  });
});
