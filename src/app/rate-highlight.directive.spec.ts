import { RateHighlightDirective } from './rate-highlight.directive';

describe('RateHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RateHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
